{
  "buildCommand": "cd memories-of-sky && npm install && npm run build",
  "outputDirectory": "memories-of-sky/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ]
}
